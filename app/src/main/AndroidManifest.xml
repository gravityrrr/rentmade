<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Made"
        tools:targetApi="36">

        <activity
            android:name=".ui.splash.SplashActivity"
            android:exported="true"
            android:theme="@style/Theme.Made.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="made" android:host="auth" android:pathPrefix="/callback" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.auth.LoginActivity" />
        <activity android:name=".ui.auth.SignupActivity" />
        <activity android:name=".ui.admin.AdminDashboardActivity" />
        <activity android:name=".ui.dashboard.DashboardActivity" android:launchMode="singleTop" />
        <activity android:name=".ui.property.PropertyPortfolioActivity" android:launchMode="singleTop" />
        <activity android:name=".ui.property.AddPropertyActivity" />
        <activity android:name=".ui.property.PropertyUnitsActivity" />
        <activity android:name=".ui.settings.SettingsActivity" android:launchMode="singleTop" />
        <activity android:name=".ui.tenant.TenantStatusActivity" android:launchMode="singleTop" />
        <activity android:name=".ui.tenant.AddTenantActivity" />
        <activity android:name=".ui.tenant.TenantDetailsActivity" />

    </application>
</manifest>